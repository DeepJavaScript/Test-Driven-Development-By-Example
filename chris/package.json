{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test": "vitest --reporter=verbose",
    "coverage": "vitest run --coverage",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^2.9.9",
    "vitest": "^0.14.1"
  }
}
